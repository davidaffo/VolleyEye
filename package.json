{
  "name": "volleyeye",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "release": "node scripts/release.mjs",
    "test": "node --test --test-isolation=none tests/*.test.mjs",
    "version:sync": "node scripts/sync-version.mjs",
    "dev": "npm run serve",
    "serve": "npx serve . --listen ${PORT:-3000}",
    "tunnel": "PORT=${PORT:-3000} bash serve-tunnel.sh"
  },
  "dependencies": {
    "marked": "^18.0.13"
  }
}
